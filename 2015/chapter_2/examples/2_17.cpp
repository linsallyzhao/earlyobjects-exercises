#include <iostream>
using namespace std;

int main()
{
    long double apple;

    cout << "The size of an integer is " << sizeof(int);
    cout << " bytes. \n";
    cout << "The size of an long integer is " << sizeof(long);
    cout << " bytes. \n";
    cout << "An apple can be earen in " << sizeof(apple);
    cout << "bytes! \n";

    return 0;
}
